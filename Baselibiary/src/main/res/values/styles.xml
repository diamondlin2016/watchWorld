<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="loading_dialog_style">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Theme.AlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <!--<item name="colorAccent">@color/colorPrimary</item>-->
        <!-- Used for the title and text -->
        <!--<item name="android:textColorPrimary">@color/theme.primary.text</item>-->
        <!-- Used for the background -->
        <item name="android:background">@color/white</item>
    </style>

    <style name="Theme.AlertDialog.Transparent" >
        <!-- Used for the buttons -->
        <!--<item name="colorAccent">@color/theme.colorPrimary</item>-->
        <!-- Used for the title and text -->
        <!--<item name="android:textColorPrimary">@color/theme.third.text</item>-->
        <!-- Used for the background -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

</resources>