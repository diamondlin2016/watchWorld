<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->

        <!--actionBar栏颜色 不能添加透明通道-->
        <item name="colorPrimary">@color/theme.colorPrimary</item>
        <!--状态栏颜色-->
        <item name="colorPrimaryDark">@color/theme.colorPrimaryDark</item>
        <!--editText线条、radioButton按钮颜色-->
        <item name="colorAccent">@color/theme.colorAccent</item>
        <item name="android:statusBarColor" tools:ignore="NewApi">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:ignore="NewApi">true</item>
        <item name="clockBackground">@android:color/white</item>
        <item name="clockTextColor">@android:color/black</item>
    </style>

    <!--夜间模式主题-->
    <style name="NightTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/theme.night.colorPrimary</item>
        <item name="colorPrimaryDark">@color/theme.night.colorPrimaryDark</item>
        <item name="colorAccent">@android:color/darker_gray</item>
        <item name="android:windowBackground">@android:color/background_dark</item>
        <item name="clockBackground">@color/theme.night.colorBackground</item>
        <item name="clockTextColor">@color/theme.night.colorText</item>
    </style>

    <style name="Theme" parent="AppTheme"/>
    <!-- Widget -->
    <style name="Theme.Widget"/>
    <style name="Theme.Widget.Toolbar">
        <item name="android:id">@id/toolbar</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?android:attr/actionBarSize</item>
        <item name="titleTextAppearance">@style/Theme.TextAppearance.Light.Toolbar.Title</item>
    </style>
    <!-- TextAppearance -->
    <style name="Theme.TextAppearance"/>

    <style name="Theme.TextAppearance.Toolbar">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="Theme.TextAppearance.Light.Toolbar.Title" parent="Theme.TextAppearance.Toolbar">
        <item name="android:textColor">@color/theme.textColorPrimary</item>
    </style>

    <style name="Theme.TextAppearance.List">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="Theme.TextAppearance.List.Title">
        <item name="android:textColor">@color/selector_list_title_text</item>
        <item name="android:textSize">@dimen/text.list.title</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="Theme.TextAppearance.List.Small">
        <item name="android:textColor">@color/selector_list_hint_text</item>
        <item name="android:textSize">@dimen/text.list.small</item>
        <item name="android:singleLine">true</item>
    </style>


</resources>
